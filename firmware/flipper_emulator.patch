diff --git a/applications/services/input/input.c b/applications/services/input/input.c
index d7e9f3d..21fd894 100644
--- a/applications/services/input/input.c
+++ b/applications/services/input/input.c
@@ -117,11 +117,20 @@ int32_t input_srv(void* p) {
         bool is_changing = false;
         for(size_t i = 0; i < input_pins_count; i++) {
             bool state = GPIO_Read(pin_states[i]);
+#ifdef FLIPPER_EMULATOR
+            /* Emulator patch: snap debounce to its terminal value in one poll.
+             * The gradual debounce needs several furi_delay_tick() self-loops,
+             * which are timing-fragile under functional emulation and make button
+             * presses register unreliably. Injected GPIO edges are already clean,
+             * so no debounce is needed. */
+            pin_states[i].debounce = state ? INPUT_DEBOUNCE_TICKS : 0;
+#else
             if(state) {
                 if(pin_states[i].debounce < INPUT_DEBOUNCE_TICKS) pin_states[i].debounce += 1;
             } else {
                 if(pin_states[i].debounce > 0) pin_states[i].debounce -= 1;
             }
+#endif
 
             if(pin_states[i].debounce > 0 && pin_states[i].debounce < INPUT_DEBOUNCE_TICKS) {
                 is_changing = true;
diff --git a/applications/services/region/region.c b/applications/services/region/region.c
index bed676f..1e8951c 100644
--- a/applications/services/region/region.c
+++ b/applications/services/region/region.c
@@ -135,6 +135,15 @@ int32_t region_on_system_start(void* p) {
         return 0;
     }
 
+#ifdef FLIPPER_EMULATOR
+    /* Emulator: skip loading the dynamic region file. It reads from internal
+     * storage (LittleFS on flash), which is not backed by a real formatted
+     * filesystem in the emulator and stalls storage_common_stat(). The firmware
+     * falls back to its built-in default region, which is fine for the emulator. */
+    UNUSED(region_load_file);
+    return 0;
+#else
     region_load_file(NULL);
     return 0;
+#endif
 }
diff --git a/applications/services/storage/storage_processing.c b/applications/services/storage/storage_processing.c
index 7a32805..3abad26 100644
--- a/applications/services/storage/storage_processing.c
+++ b/applications/services/storage/storage_processing.c
@@ -79,6 +79,16 @@ static FS_Error storage_get_data(Storage* app, FuriString* path, StorageData** s
             type = ST_EXT;
         }
 
+#ifdef FLIPPER_EMULATOR
+        /* Emulator: internal storage (LittleFS on flash) is not backed by a real
+         * filesystem. Any /int access must fail fast with FSE_NOT_READY so the
+         * firmware falls back to defaults instead of stalling in the storage
+         * service (the ST_INT StorageData has no fs_api). */
+        if(type != ST_EXT) {
+            return FSE_NOT_READY;
+        }
+#endif
+
         furi_assert(type == ST_EXT);
 
         if(storage_data_status(&app->storage[type]) != StorageStatusOK) {
diff --git a/targets/f7/furi_hal/furi_hal_bt.c b/targets/f7/furi_hal/furi_hal_bt.c
index f1881f9..009d705 100644
--- a/targets/f7/furi_hal/furi_hal_bt.c
+++ b/targets/f7/furi_hal/furi_hal_bt.c
@@ -128,7 +128,17 @@ bool furi_hal_bt_start_radio_stack(void) {
 
     furi_hal_bt_unlock_core2();
 
+#ifdef FLIPPER_EMULATOR
+    /* Emulator: Core2 (BLE M0+) is not emulated, so the radio stack never
+     * starts (res == false). gap_extra_beacon_init() would then send an SHCI
+     * command over IPCC to the absent Core2 and block in HW_IPCC_SYS_SendCmd
+     * until its 33s timeout -> furi_crash. Skip it when the stack isn't up. */
+    if(res) {
+        gap_extra_beacon_init();
+    }
+#else
     gap_extra_beacon_init();
+#endif
     return res;
 }
 
diff --git a/targets/f7/furi_hal/furi_hal_i2c.c b/targets/f7/furi_hal/furi_hal_i2c.c
index 7eb9b49..ed712f1 100644
--- a/targets/f7/furi_hal/furi_hal_i2c.c
+++ b/targets/f7/furi_hal/furi_hal_i2c.c
@@ -52,6 +52,25 @@ void furi_hal_i2c_release(const FuriHalI2cBusHandle* handle) {
 
 static bool
     furi_hal_i2c_wait_for_idle(I2C_TypeDef* i2c, FuriHalI2cBegin begin, FuriHalCortexTimer timer) {
+#ifdef FLIPPER_EMULATOR
+    /* Emulator patch: there is no real I2C bus under functional emulation, so
+     * the STM32 I2C peripheral never raises the TXIS/RXNE/STOPF/TC flags the
+     * transfer loop polls for. Without this guard every access to the battery
+     * gauge (bq27220 @0xAA on I2C1) and the LED driver (lp5562) spins in a busy
+     * wait until its DWT timeout, and the power service repeats that poll
+     * forever. That starves the FreeRTOS idle path so SysTick / the software
+     * timer daemon never advance and the idle dolphin animation freezes.
+     *
+     * Failing the very first step of every transaction turns each I2C read/write
+     * into a fast, graceful failure (the drivers already handle it: the gauge
+     * reports defaults, the LED driver no-ops). Boot is much faster and the
+     * animation timer ticks. This is the entry point of furi_hal_i2c_transaction
+     * so the remaining helpers stay compiled but are simply never reached. */
+    UNUSED(i2c);
+    UNUSED(begin);
+    UNUSED(timer);
+    return false;
+#else
     do {
         if(furi_hal_cortex_timer_is_expired(timer)) {
             return false;
@@ -60,6 +79,7 @@ static bool
     // Only check if the bus is busy if starting a new transaction, if not we already control the bus
 
     return true;
+#endif
 }
 
 static bool
diff --git a/targets/f7/furi_hal/furi_hal_power.c b/targets/f7/furi_hal/furi_hal_power.c
index fe5c0cf..46ee9e8 100644
--- a/targets/f7/furi_hal/furi_hal_power.c
+++ b/targets/f7/furi_hal/furi_hal_power.c
@@ -70,6 +70,14 @@ void furi_hal_power_init(void) {
 #endif
 
     furi_hal_i2c_acquire(&furi_hal_i2c_handle_power);
+#ifdef FLIPPER_EMULATOR
+    /* Emulator patch: the battery gauge (bq27220) and charger (bq25896) are not
+     * emulated. Their init fails and the stock code waits furi_delay_us(4000000)
+     * per retry, which is minutes of wall-clock in the emulator. Skip the probe
+     * and mark them absent; power reads fall back to safe defaults elsewhere. */
+    furi_hal_power.gauge_ok = false;
+    furi_hal_power.charger_ok = false;
+#else
     // Find and init gauge
     size_t retry = 2;
     while(retry > 0) {
@@ -98,6 +106,7 @@ void furi_hal_power_init(void) {
         }
         retry--;
     }
+#endif
     furi_hal_i2c_release(&furi_hal_i2c_handle_power);
 
     FURI_LOG_I(TAG, "Init OK");
@@ -160,7 +169,17 @@ void furi_hal_power_insomnia_exit(void) {
 }
 
 bool furi_hal_power_sleep_available(void) {
+#ifdef FLIPPER_EMULATOR
+    /* Emulator patch: disable tickless-idle deep sleep. It relies on the LPTIM1
+     * wake-up interrupt to resume threads that call furi_delay_tick(); under
+     * functional emulation that wake path is unreliable and starves the input
+     * debounce loop (buttons never register). Returning false makes the idle
+     * task use plain __WFI(), which Renode wakes on the SysTick interrupt, so
+     * furi_delay_tick() and button debounce work normally. */
+    return false;
+#else
     return furi_hal_power.insomnia == 0;
+#endif
 }
 
 static inline bool furi_hal_power_deep_sleep_available(void) {
diff --git a/targets/f7/furi_hal/furi_hal_sd.c b/targets/f7/furi_hal/furi_hal_sd.c
index 1521927..82c46d1 100644
--- a/targets/f7/furi_hal/furi_hal_sd.c
+++ b/targets/f7/furi_hal/furi_hal_sd.c
@@ -217,6 +217,14 @@ static inline void sd_spi_deselect_card(void) {
 }
 
 static void sd_spi_bus_to_ground(void) {
+#ifdef FLIPPER_EMULATOR
+    /* Emulator patch: the physical power-cycle of the SD card is done by
+     * bit-banging MISO/MOSI/SCK as GPIO. Renode emulates at the functional
+     * (not cycle) level and cannot translate those GPIO toggles into the SPI
+     * byte stream, which corrupts SD init. Keep the pins on the SPI peripheral
+     * and skip the bus grounding entirely. */
+    return;
+#else
     furi_hal_gpio_init_ex(
         furi_hal_sd_spi_handle->miso,
         GpioModeOutputPushPull,
@@ -240,9 +248,17 @@ static void sd_spi_bus_to_ground(void) {
     furi_hal_gpio_write(furi_hal_sd_spi_handle->miso, false);
     furi_hal_gpio_write(furi_hal_sd_spi_handle->mosi, false);
     furi_hal_gpio_write(furi_hal_sd_spi_handle->sck, false);
+#endif
 }
 
 static void sd_spi_bus_rise_up(void) {
+#ifdef FLIPPER_EMULATOR
+    /* Emulator patch: counterpart of sd_spi_bus_to_ground(). The SPI pins were
+     * never taken to GPIO, so there is nothing to restore; just make sure the
+     * card is deselected. */
+    sd_spi_deselect_card();
+    return;
+#else
     sd_spi_deselect_card();
 
     furi_hal_gpio_init_ex(
@@ -263,6 +279,7 @@ static void sd_spi_bus_rise_up(void) {
         GpioPullUp,
         GpioSpeedVeryHigh,
         GpioAltFn5SPI2);
+#endif
 }
 
 static inline uint8_t sd_spi_read_byte(void) {
diff --git a/targets/f7/furi_hal/furi_hal_spi.c b/targets/f7/furi_hal/furi_hal_spi.c
index 9997d27..20bfac7 100644
--- a/targets/f7/furi_hal/furi_hal_spi.c
+++ b/targets/f7/furi_hal/furi_hal_spi.c
@@ -201,6 +201,15 @@ bool furi_hal_spi_bus_trx_dma(
     furi_check(handle->bus->current_handle == handle);
     furi_check(size > 0);
 
+#ifdef FLIPPER_EMULATOR
+    /* Emulator patch: the DMA-complete interrupt path (waiting on the
+     * spi_dma_completed semaphore) depends on the SPI<->DMA request wiring and
+     * the DMA transfer-complete IRQ, which are not modelled in Renode. That
+     * makes the semaphore time out and furi_check() crash on SD block reads.
+     * The polling (non-DMA) transfer works fine, so always use it. */
+    return furi_hal_spi_bus_trx(handle, tx_buffer, rx_buffer, size, timeout_ms);
+#endif
+
     // If scheduler is not running, use blocking mode
     if(!furi_kernel_is_running()) {
         return furi_hal_spi_bus_trx(handle, tx_buffer, rx_buffer, size, timeout_ms);
